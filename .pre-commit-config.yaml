# Run: uv run pre-commit install   (once, to enable the git hook)
#      uv run pre-commit run --all-files
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.16
    hooks:
      - id: ruff          # lint, autofix what it safely can
        args: [--fix]
      - id: ruff-format   # formatting

  - repo: local
    hooks:
      # Run via uv so mypy resolves the project's deps (and their type stubs);
      # config lives in python/pyproject.toml, hence the cd.
      - id: mypy
        name: mypy
        entry: bash -c 'cd python && uv run mypy'
        language: system
        pass_filenames: false
        files: ^python/.*\.py$

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
